[project]
name = "predictive-maintenance-ml"
version = "0.1.0"
description = "Production-oriented predictive maintenance ML system"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
